<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <RadioGroup
        android:id="@+id/rg_tab"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#ff8fc31f"
        android:orientation="horizontal" >

        <RadioButton
            android:id="@+id/radiobutton_moni"
            style="@style/top_tab"
            android:drawableLeft="@drawable/radio_monitor_icon_2"
            android:text="监测" />

        <RadioButton
            android:id="@+id/radiobutton_control"
            style="@style/top_tab"
            android:drawableLeft="@drawable/radio_control_icon_2"
            android:text="控制" />

        <RadioButton
            android:id="@+id/radiobutton_video"
            style="@style/top_tab"
            android:drawableLeft="@drawable/radio_video_icon_2"
            android:drawablePadding="0dp"
            android:text="视频" />
    </RadioGroup>

    <RelativeLayout
        android:id="@+id/detail_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/rg_tab" >
    </RelativeLayout>

</RelativeLayout>